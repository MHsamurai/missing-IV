\documentclass[a4paper,10pt]{article}
\usepackage{xeCJK}
\setCJKmainfont{Hiragino Mincho ProN}
\setCJKsansfont{Hiragino Sans}
\setCJKmonofont{Hiragino Sans}

\usepackage[top=18mm,bottom=20mm,left=17mm,right=17mm]{geometry}
\usepackage{amsmath,amssymb,bm}
\usepackage{multicol}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usetikzlibrary{arrows.meta,positioning,fit,calc,shapes.geometric}
\captionsetup{font=small,labelfont=bf,hypcap=false}

\pagestyle{plain}
\setlength{\parindent}{1em}
\setlength{\parskip}{0.25em}
\setlength{\columnsep}{7mm}
\setlength{\emergencystretch}{3em}
\renewcommand{\baselinestretch}{1.05}

\titleformat{\section}
  {\normalfont\bfseries\large}
  {\thesection.}{0.45em}{}
\titlespacing*{\section}{0pt}{1.4\baselineskip}{0.45\baselineskip}

\newcommand{\E}{\mathbb E}
\newcommand{\Prb}{\mathbb P}
\newcommand{\indep}{\mathrel{\perp\!\!\!\perp}}
\newcommand{\Y}{\bm Y}
\newcommand{\Z}{\bm Z}
\newcommand{\one}{\bm 1}

\newcommand{\papertitle}{欠測IVと潜在変数モデリングによる非無作為欠測の識別}
\newcommand{\authorblock}{%
  \begin{tabular}{c}
    本多 将大\(^{1,2}\)、星野 崇宏\(^{1,2}\)、大津 泰介\(^{1,3}\)\\[-0.1mm]
    {\scriptsize 1. 慶應義塾大学、2. 理化学研究所、3. London School of Economics and Political Science}
  \end{tabular}
}

\newcommand{\draftprompt}[1]{%
  \noindent{\color{gray}\footnotesize #1}\par\vspace{1.5mm}
}

\begin{document}

\begin{center}
  {\LARGE\bfseries \papertitle\par}
  \vspace{2mm}
  {\normalsize \authorblock}
\end{center}
\vspace{3mm}

\begin{multicols}{2}

\section{問題設定}
医学・疫学・心理学の実証研究では，複数の検査項目が欠測するだけでなく，
それらが測定する認知機能，疾患重症度，抑うつ，生理学的状態などの
構成概念そのものが潜在的であることが多い。例えば，認知症研究における
選択的な認知検査不能，Parkinson病研究における運動・認知・
バイオマーカーの選択的取得，診療記録における重症度依存の検査オーダー，
質問票におけるセンシティブ項目の非回答では，個々の項目平均だけでなく，
潜在クラスの構成比と，各項目が潜在状態をどう測定するかを表す測定核を
知りたい。この識別対象は，因子分析，構造方程式モデリング
(structural equation modeling; SEM)，項目反応理論
(item response theory; IRT)に共通する潜在測定モデルの中心である
（Muth\'en et al., 1987; Holman and Glas, 2005; Lee and Tang, 2006;
Kuha et al., 2018）。

本研究では，未観測潜在変数$F$，常時観測変数$W,X$，複数の潜在的測定値
$Y=(Y_1,\ldots,Y_m)$について，
\begin{align}
F\mid(W,X)&\sim Q_F(\cdot\mid W,X),\\
Y_j\mid(F,X)&\sim Q_j(\cdot\mid F,X;\theta_j),\\
Y_1,\ldots,Y_m&\indep\mid(F,X)
\end{align}
という潜在測定モデルを考える。識別したいのは潜在分布$Q_F$と測定核
$Q_Y=\{Q_j:j=1,\ldots,m\}$である。しかし$Y_j$は観測指標$D_j=1$の
ときだけ観測され，$D=(D_1,\ldots,D_m)$は未観測の$Y$や$F$に依存して
よい。したがって問題は，missing not at random (MNAR)の下でfull lawを
回復することにとどまらず，
回復可能な観測変数の法則を潜在分布と測定核へ一意に分解できるか，である。

通常のinstrumental variable (IV)法は，内生変数を動かす外生変動を用いて
構造パラメータを識別する。これに対しMNAR欠測では，未観測の結果とその
観測指標が同時に問題となるため，通常のIV法や結果分布・欠測機構を規定する
model-based approachだけからfull lawが識別されるとは限らない。近年は，
欠測結果$Y$と関連するが，$(Y,U)$を条件とすれば観測指標$R$と独立な
常時観測変数$Z$をmissingness IV，またはshadow variableとして用いる。
この除外制約にpositivityとcompletenessを組み合わせ，選択確率とfull law，
または回帰パラメータを識別する研究が進められている
（d'Haultfoeuille, 2010; Zhao and Shao, 2015）。

しかし本稿の問題では，潜在変数を従来のshadow-IVモデルのように単に
積分消去し，既存のmissing-IVモデルへ帰着することはできない。仮に
常時観測変数$W$について
\[
D\indep W\mid(Y,F,X)
\]
が成立しても，
\begin{align}
&P(D=d\mid Y=y,W=w,X=x) \notag\\
&\qquad=
\int P(D=d\mid y,f,x)\,dP(f\mid y,w,x).
\end{align}
である。$W$が$F$を予測すれば$P(f\mid y,w,x)$は一般に$w$に依存するため，
$D\indep W\mid(Y,X)$は従わない。すなわち，$F$を条件に含む除外制約は，
$F$を積分した観測データ上の除外制約を一般には導かない。反対に，$F$を
最初から消去して観測変数のfull lawだけを回復しても，その法則を生成した
$Q_F$と$Q_Y$の分解は自動的には識別されない。これが，既存のスカラー
missing-IV識別を潜在測定モデルへ拡張するときの固有の障害である。

関連する既存研究は，この障害の二つの側面を別々に扱ってきた。上記の
missing-IV研究がMNAR selectionの除去を扱う一方，Allman, Matias and
Rhodes (2009) は，欠測による選択を伴わない潜在構造
モデルにおいて，多数の顕在変数から有限潜在クラスの分布と測定核を
識別する。潜在変数を含むMNARモデルも因子分析・SEM・IRTで提案されて
いるが，多くは欠測機構と潜在測定モデルを同時にパラメトリックに規定する。
少なくともこれら二系列の既存結果は，部分的にしか同時観測されない多次元
測定について，missing IVで欠測機構を除去し，さらに同じ観測データ法則から
潜在分布と測定核までを識別する統合定理を与えていない。したがって，
missing IVによるfull-law回復と潜在構造の一意分解を接続する必要がある。

本稿の問題設定は次の通りである。まず，同時観測確率が正である
supported blockごとに，観測可能な条件付け集合$(Y_S,U_S)$に対する
missingness IV $Z_S$を用いてinverse-selection bridgeを構成し，block lawを
回復する。ここで$U_S=(X,W)$とし，潜在分布$Q_F(\cdot\mid W,X)$を動かす
latent shifter $W$をmissingness IVから分離する。次に，回復したoverlapping
block lawsを共有anchor itemsで接続し，潜在クラスのlabelをblock間で
整合させる。この二段階を通じて，global complete caseが存在しない場合にも，
潜在クラス分布$Q_F$と測定核$Q_Y$を識別する条件を与える。

この構成は三つの識別原理を引き継ぐ。第一に，Zhao and Shao (2015) の
共変量調整済み除外制約を，各supported block $S$の
$(Y_S,U_S,Z_S,R_S)$へ適用する。第二に，d'Haultfoeuille
(2010, Assumptions 1--5, Theorem 2.3) のpositivityとB-completenessを
用い，bridgeの一意性を通じて各blockの選択確率$\pi_S$とfull law
$P(Y_S,Z_S,U_S)$をノンパラメトリックに回復する。第三に，Allman,
Matias and Rhodes (2009, Sections 2--5) の有限潜在クラスに対する
tensor分解の識別論理を引き継ぎ，supported blocks上で回復されたtensorへ
適用する。本稿の新しい接続は，第一段階でMNAR selectionを除去し，
第二段階でanchor条件の下に潜在分布と測定核を一意分解する点にある。

したがって，$Z_S$には$Y_S$を予測するrelevanceと
$R_S\indep Z_S\mid(Y_S,U_S)$というexclusionが必要である。一方，$W$は
測定核へ直接入らず，潜在分布を動かすvariationを供給する。どの変数を
blockwise IV，latent shifter，通常共変量に割り当て，どの項目をblock間の
anchorにするかが，応用上の識別設計の核心となる。

\section{モデルと仮定}
本節は識別を二段階に分け，その第一段階を定式化する。まず潜在因子$F$，
測定値$Y$，常時観測共変量$(X,W,Z)$，観測指標$D$からなる完全データ法則を
定める。次に，同時観測確率が正であるsupported blockごとに
Zhao--Shao型exclusionとd'Haultfoeuille型B-completenessを課し，
観測データ法則から選択確率$\pi_S$とblock full lawを回復する。この段階では
block lawを潜在クラスへ分解せず，欠測機構の除去だけを識別目標とする。

\noindent\textbf{仮定1（観測過程）.}
$D=(D_1,\ldots,D_m)\in\{0,1\}^m$，$Y_j$は$D_j=1$のときだけ観測される。
$X,W,Z,D$は常時観測され，
\[
O=(X,W,Z,D,Y_D),\qquad Y_D=\{Y_j:D_j=1\}.
\]
ここで$Z$は必要なblockごとのshadow $Z_S$を含む情報の集合である。

\noindent\textbf{仮定2（潜在分布のshift）.}
$F\mid(W,X)\sim Q_F(\cdot\mid W,X)$。

\noindent\textbf{仮定3（測定過程）.}
$Y_j\mid(F,X)\sim Q_j(\cdot\mid F,X;\theta_j)$，$j=1,\ldots,m$。

\noindent\textbf{仮定4（局所独立性とexclusion）.}
\[
P(Y\mid F,W,X,Z)=\prod_{j=1}^m Q_j(Y_j\mid F,X).
\]
すなわち$Y_1,\ldots,Y_m$は$(F,X)$の下で条件付き独立であり，$W$は
測定核へ直接入らない。$Z_S$も測定核へ直接入らないが，$(W,X)$の下で
$F$と関連し，潜在分布を通じて$Y_S$を予測してよい。従って
$F\mid(W,X)\sim Q_F$は$Z$を周辺化した分布であり，完全データ法則では
$P(F,Z\mid W,X)$を制限しない。

\begin{center}
\begin{minipage}{\linewidth}
\centering
\input{notes/figures/latent_mnar_dag.tex}
\captionof{figure}{単一supported blockにおける潜在測定・欠測機構。
円は潜在変数，四角は顕在変数，diamondはblock観測指標を表す。
$U_S=(X,W)$は共変量として$F,Z_S,Y_S$へ入り，$Z_S$は$F$を通じて
$Y_S$と関連する。$R_S\indep Z_S\mid(Y_S,U_S)$より，$Z_S$から$R_S$への
直接辺は置かない。}
\label{fig:latent-mnar-dag}
\end{minipage}
\end{center}

\noindent\textbf{仮定5（supported block）.}
$S\subseteq\{1,\ldots,m\}$について
\[
R_S=\prod_{j\in S}D_j,\qquad Y_S=(Y_j:j\in S),\qquad U_S=(X,W).
\]
$R_S=1$なら$Y_S$全体が観測される。block観測データは
$O_S=(U_S,Z_S,R_S,Y_S^{\mathrm{obs}})$であり，
\[
Y_S^{\mathrm{obs}}=
\begin{cases}Y_S,&R_S=1,\\ \dagger,&R_S=0\end{cases}
\]
とする。
$S$がsupportedであるとは，対象領域で
\[
\pi_S(y,u):=P(R_S=1\mid Y_S=y,U_S=u)>0
\]
が成立することをいう。

記号の対応は，Zhao and Shao (2015)の$(Y,U,Z,R)$をblockごとの
$(Y_S,U_S,Z_S,R_S)$に置き換えたものである。とくに$W$は$F$の分布を
動かすlatent shifterなので，nonresponse IV $Z_S$とは区別して$U_S$へ
含め，その値を条件づけた上で除外制約を課す。

\noindent\textbf{ノンパラメトリックなbenchmarkと共変量調整.}
d'Haultfoeuille (2010)の全ベクトルを直接扱うrouteは，
$R_{[m]}=\prod_{j=1}^mD_j$の
positivityと
\[
\E\{g(Y,U)\mid Z,U\}=0\ \Longrightarrow\ g(Y,U)=0
\]
というglobal B-completenessを要する。exact top-$K<m$では
$R_{[m]}=0$ a.s.なので，以下のblockwise formulationを用いる。

\noindent\textbf{仮定6（Zhao--Shao型blockwise covariate-adjusted missing IV）.}
各supported $S$について，
\[
\begin{aligned}
\relax[R_S\mid Y_S,U_S,Z_S]
  &=[R_S\mid Y_S,U_S],\\
R_S&\indep Z_S\mid(Y_S,U_S).
\end{aligned}
\]
ここで第2式は第1式の独立性による表記である。
従って任意の可測集合$A$について
\[
P(Z_S\in A\mid Y_S,U_S,R_S=1)
=P(Z_S\in A\mid Y_S,U_S),
\]
これはZhao and Shao (2015)のcomplete-case identity
$[Z\mid Y,U,R=1]=[Z\mid Y,U]$のblockwise analogueである。

\noindent\textbf{仮定7（blockwise conditional B-completeness）.}
各supported $S$について，任意の下に有界かつ可積分な$g$に対し
\[
\E\{g(Y_S,U_S)\mid Z_S,U_S\}=0
\Longrightarrow g(Y_S,U_S)=0\quad\text{a.s.}
\]
が成立する。global and blockwise formulationsの間に一般的な包含関係はない。
Zhao and Shao (2015)では一般化線形モデルの有限次元パラメータに対して
Jacobian行列のfull-rank条件を用いる。本稿では$P(Y_S\mid U_S)$を
パラメトリックに限定せずblock law全体を回復するため，対応する
無限次元の単射条件としてB-completenessを仮定する。

\begin{center}
\begin{minipage}{\linewidth}
\centering
\resizebox{\linewidth}{!}{\input{notes/figures/supported_blocks_dag.tex}}
\captionof{figure}{複数のsupported blocksにおける多次元missing IV，
outcome，block indicatorの構造。各外枠は一つのsupported blockを表し，
内部の小さな四角は$\bm Z_S$と$Y_S$の各成分，diamondは
$R_S=\prod_{k\in S}D_k$を表す。anchor items $Y_a,Y_b$をblock間で
含む$S_0=\{a,b\}$がanchor pairを構成し，各extension pair
$S_j=\{j,a\}$は$Y_a$を共有して測定核を伝播する。縦の省略記号は
$S_0,S_1,\ldots,S_J$のsupported pairsを表す。各blockで
$U_S$は$F,\bm Z_S,Y_S$へ入り，$\bm Z_S$から$R_S$への直接辺は置かない。
灰色はextension pairsに共通するanchor item $Y_a$を表す。}
\label{fig:supported-blocks-dag}
\end{minipage}
\end{center}

\noindent\textbf{命題2.1（supported-block lawの識別）.}
仮定1，5，6，7の下で，$\pi_S$は
\begin{equation}
\E\left\{\frac{R_S}{q(Y_S,U_S)}\mid Z_S,U_S\right\}=1
\end{equation}
を満たし条件付き期待値が有限な正値関数$q$の唯一解である。従って任意の可積分$h$に対し
\begin{equation}
\E h(Y_S,Z_S,U_S)
=\E\left\{\frac{R_Sh(Y_S,Z_S,U_S)}{\pi_S(Y_S,U_S)}\right\},
\end{equation}
よって$P(Y_S,Z_S,U_S)$が識別される。仮定6の共変量調整済み除外制約は
Zhao and Shao (2015)型であり，$q$の一意性とfull lawの回復は
d'Haultfoeuille (2010, Theorem 2.3)型である。命題は両者を各supported
blockへ条件付き適用した拡張になっている。

\noindent\textbf{証明.}
排除条件より
\[
\E(R_S\mid Y_S,Z_S,U_S)=\pi_S(Y_S,U_S),
\]
従って真の$\pi_S$は(5)を満たす。別の正値解$q$に対し
$g=\pi_S/q-1$と置くと，
\[
0=\E\left[R_S\left\{q^{-1}-\pi_S^{-1}\right\}\mid Z_S,U_S\right]
=\E(g\mid Z_S,U_S).
\]
(5)を周辺化すれば$\E(\pi_S/q)=1$なので$g\ge-1$かつ
$\E|g|\le2$。B-completenessより$g=0$ a.s.，従って$q=\pi_S$。
最後に
\[
\E\left\{R_Sh/\pi_S\mid Y_S,Z_S,U_S\right\}=h
\]
を周辺化すれば(6)を得る。\hfill$\square$

\begin{center}
\begin{minipage}{\linewidth}
\centering
\input{notes/figures/identification_flow.tex}
\captionof{figure}{観測データ法則から潜在関数までの識別フロー。
命題2.1が各supported blockの選択確率とfull lawを回復し，その周辺化から
block tensorを構成する。条件付き独立な3-view tensorのKruskal一意性と
$W$を第三modeとする線形反転により，anchor orderingの下で
$Q_F(x)$と$Q_Y(x)$を識別する。矢印は因果効果ではなく識別写像である。}
\label{fig:identification-flow}
\end{minipage}
\end{center}

命題2.1により，観測データ法則から各supported blockのfull lawへ至る
第一段階の識別は完結する。ただし，回復されたblock tensorsをどの潜在
分布と測定核が生成したかは，この命題だけでは一意にならない。残る問題は，
overlapping blocksの分解を同時に一意化し，blockごとのclass labelを
整合させることである。第3節では，anchor pairと常時観測される$W$を
三つのviewとして用い，この第二段階を有限潜在クラスについて解く。

\section{有限潜在クラスの統合識別}
本節は，Allman, Matias and Rhodes (2009, Sections 2--5) における有限
潜在クラスの識別論理を引き継ぎ，supported blocks上のMNAR識別問題へ
適用する。同論文は，$F$の下で条件付き独立な三つの顕在viewが生成する
probability tensorを構成し，その分解をKruskal rank条件の下で一意化する。
本稿では第三の測定項目を必要とせず，二つのanchor items $Y_a,Y_b$と
latent shifter $W$を三つのviewとする。命題2.1で回復したpair lawsと
常時観測される$W$から$P(Y_a,Y_b,W\mid X)$を構成し，Allman et al.と
同じ三方向配列のrouteを適用する。

以下，$P_X(\mathcal X_0)=1$となる共通集合$\mathcal X_0$を取り，任意の
$x\in\mathcal X_0$を固定する。
$F\in\{1,\ldots,r\}$，$r\ge2$，$d_j=|\mathcal Y_j|$とし，$W$の有限
supportを$\mathcal W_x=\{w_1,\ldots,w_L\}$とする。測定行列，class比率，
$W$のclass-specific lawを
\begin{align*}
M_j(y,f;x)&=P(Y_j=y\mid F=f,X=x),\\
p_f(x)&=P(F=f\mid X=x),\\
G(w,f;x)&=P(W=w\mid F=f,X=x)
\end{align*}
と置く。$M_j$と$G$の各列は確率ベクトルである。また
\[
\begin{aligned}
\lambda_f(w,x)
 &=P(F=f\mid W=w,X=x)\\
 &=\frac{p_f(x)G(w,f;x)}{P(W=w\mid X=x)}.
\end{aligned}
\]
識別対象は$Q_Y(x)=\{M_j(x):j=1,\ldots,m\}$と
$Q_F(x)=\{\lambda(w,x):w\in\mathcal W_x\}$である。

パラメータ空間を
\[
\Theta_x=\Delta_{r-1}\times(\Delta_{L-1})^r
\times\prod_{j=1}^m(\Delta_{d_j-1})^r
\]
とする。$\mathcal T_{jkW}(x)$を要素
$P(Y_j=u,Y_k=v,W=w_\ell\mid X=x)$からなる$d_j\times d_k\times L$
配列とし，$P_{jk}(w,x)=[P(Y_j=u,Y_k=v\mid W=w,X=x)]_{u,v}$と書く。
$\Phi_x$を$\Theta_x$から
$\{\mathcal T_{abW},\mathcal T_{jaW}:j\notin\{a,b\}\}$への写像とする。
識別とは，class permutationを除いて$\Phi_x$が単射であることをいう。
以下では実現した行列にrank条件を直接課し，その下でstrict identificationを
示す。

\noindent\textbf{補題3.1（Khatri--Rao rank）.}
$r$列を持つ行列$A,B$のcolumn Kruskal rankを$k_A,k_B$とすると，
\[
k_{B\odot A}\ge\min\{k_A+k_B-1,r\}.
\]
特に$k_A+k_B\ge r+1$なら$B\odot A$はfull column rankである。

\noindent\textbf{証明.}
$s\le\min\{k_A+k_B-1,r\}$本以下の列に非自明な線形関係があると仮定し，
非零係数を持つ列の添字集合を$C$，$s=|C|$とする。このとき
$A_C\operatorname{diag}(c_C)B_C^\top=0$である。一方，Sylvesterのrank
不等式とKruskal rankの定義から，$s\ge1$に対し
\[
\begin{aligned}
&\operatorname{rank}\{A_C\operatorname{diag}(c_C)B_C^\top\}\\
&\qquad\ge\min(k_A,s)+\min(k_B,s)-s\ge1,
\end{aligned}
\]
となり矛盾する。従って任意の$s$列は一次独立である。\hfill$\square$

\noindent\textbf{証明方針.}
Allman et al.のrouteに沿い，(1) supported pair lawsを回復し，
(2) $Y_a,Y_b,W$の三方向配列を構成し，(3) そのcanonical polyadic
(CP)分解を一意化する。
次に，(4) 確率正規化とanchor orderingでscalingとlabelを固定し，
(5) Khatri--Rao積のrankを確立し，(6) Bayes則で各$w$の潜在クラス分布を
回復する。最後に，(7) extension pair tensorsを線形反転して全測定核を
回復し，(8) 潜在測定モデルの制約の下で未同時観測の結合分布を構成する。

\noindent\textbf{仮定3.1（covariate-assisted anchor-pair構造）.}
$r$は既知で，以下の条件が全ての$x\in\mathcal X_0$で成立する。
仮定2--4が成立し，各$f$で$p_f(x)>0$，各
$w\in\mathcal W_x$で$P(W=w\mid X=x)>0$とする。anchor pair
$\{a,b\}$と，各$j\notin\{a,b\}$に対するextension pair $\{j,a\}$は
全ての$w\in\mathcal W_x$でsupportedであり，仮定6--7を満たす。
$M_a,M_b,G$のcolumn Kruskal rankを
$k_a,k_b,k_G$として
\[
k_a+k_b+k_G\ge2r+2
\]
が成立する。既知score $s_a$に対する
$\mu_{af}(x)=\sum_y s_a(y)M_a(y,f;x)$について
$\mu_{a1}(x)<\cdots<\mu_{ar}(x)$という同じ厳密順序が
$\mathcal X_0$上で成立するとし，class labelを固定する。

\noindent\textbf{定理3.1（$W$を第三modeとするanchor-pair識別）.}
仮定3.1の下で，$\Phi_x$はanchor orderingで正規化した$\Theta_x$上で
単射である。従って観測データ法則から$p(x)$，$G(x)$，$Q_F(x)$，
$Q_Y(x)$が識別される。さらに任意の$A\subseteq\{1,\ldots,m\}$と
$w\in\mathcal W_x$について
\begin{equation}
\begin{aligned}
&P(Y_A=y_A\mid W=w,X=x)\\
&\qquad=\sum_{f=1}^r\lambda_f(w,x)
 \prod_{j\in A}M_j(y_j,f;x)
\end{aligned}
\label{eq:latent-fulljoint-pair}
\end{equation}
が識別される。これは$Y_A$が一度も同時観測されない場合も含む。

\noindent\textbf{証明.}
\textbf{Step 1（supported pair lawsの回復）.}
命題2.1を$\{a,b\}$と各$\{j,a\}$へ適用すると，
$P(Y_a,Y_b,W,X)$と$P(Y_j,Y_a,W,X)$が識別される。従って正の
$P(W=w_\ell\mid X=x)$上で条件付け・周辺化することにより，
$\mathcal T_{abW}(x)$と各$\mathcal T_{jaW}(x)$が識別される。

\textbf{Step 2（三方向配列のlatent-class表現）.}
仮定4の局所独立性とmeasurement exclusionより，$X=x$を条件として
$Y_a,Y_b,W$は$F$の下で相互に独立である。従って
\begin{equation}
\mathcal T_{abW}(x)
=\sum_{f=1}^r p_f(x)M_a(:,f;x)\otimes M_b(:,f;x)\otimes G(:,f;x).
\end{equation}
これはAllman et al. (2009, Sections 3--4)の三顕在変数latent-class
tensorと同じ形である。

\textbf{Step 3（三方向分解の一意性）.}
$\widetilde G(:,f;x)=p_f(x)G(:,f;x)$と置く。$p_f(x)>0$なので
$\widetilde G$のKruskal rankは$k_G$に等しい。仮定3.1のrank条件と
Allman et al. (2009, Theorem 1)をStep 2のtensorへ適用すると，
$M_a,M_b,\widetilde G$は，各classに共通する置換と，積が1となる列ごとの
三因子scalingを除いて一意である。

\textbf{Step 4（scalingとclass labelの固定）.}
$M_a,M_b$の各列は確率ベクトルなので，列和を1にする正規化が最初の
二因子のscalingを一意に固定する。残る第三因子について
\[
p_f(x)=\one^\top\widetilde G(:,f;x),\qquad
G(:,f;x)=\widetilde G(:,f;x)/p_f(x)
\]
とすれば，$p_f$と$G$も一意に回復される。最後に$\mu_{af}$の狭義順序が
共通置換を固定する。従って$M_a,M_b,p,G$が識別される。

\textbf{Step 5（線形反転に必要なrank）.}
$k_b\le r$とrank条件から$k_a+k_G\ge r+2$であり，補題3.1より
$G\odot M_a$はfull column rankである。同様に$k_G\le r$から
$k_a+k_b\ge r+2$であり，$M_b\odot M_a$もfull column rankである。

\textbf{Step 6（各$w$における潜在クラス分布）.}
$s_w(x)=P(W=w\mid X=x)$は観測データ法則から識別され正である。
Step 4で$p,G$が識別されたので，Bayes則
\[
\lambda_f(w,x)=p_f(x)G(w,f;x)/s_w(x)
\]
により全ての$w\in\mathcal W_x$で$\lambda(w,x)$が識別される。
同値に，Step 5を用いて
$\operatorname{vec}P_{ab}(w,x)=(M_b\odot M_a)\lambda(w,x)$を
左反転しても同じ$\lambda(w,x)$を得る。

\textbf{Step 7（非anchor測定核の逐次回復）.}
$j\notin\{a,b\}$について$\mathcal T_{jaW}$のmode-$j$展開を取ると
\[
T_{j\mid aW}(x)
=M_j(x)\operatorname{diag}\{p(x)\}
\{G(x)\odot M_a(x)\}^\top.
\]
Step 5と$p_f(x)>0$より右辺第二因子はfull row rankで右逆を持つ。
従って
\[
M_j(x)=T_{j\mid aW}(x)
\left[\operatorname{diag}\{p(x)\}
\{G(x)\odot M_a(x)\}^\top\right]^+
\]
として一意に回復される。ここで$^+$はMoore--Penrose逆行列である。
これを各$j$へ適用すれば$Q_Y(x)$全体が得られる。

\textbf{Step 8（未同時観測の結合分布）.}
Step 6--7で識別した$\lambda(w,x)$と$M_j(x)$を仮定4の局所独立性へ
代入すれば\eqref{eq:latent-fulljoint-pair}を得る。従って$|A|$が
supported blockの最大サイズを
超える場合にも，潜在測定モデルの制約の下で
$P(Y_A\mid W=w,X=x)$は一意に識別される。\hfill$\square$

\noindent\textbf{仮定3.2（連続因子の場合の正規化；定理3.1の外）.}
$F\in\mathbb R$なら，各$x$で$\E(F\mid X=x)=0$，
$\operatorname{Var}(F\mid X=x)=1$とし，anchor responseの条件付き平均を
$F$について狭義単調増加として符号を固定する。これは位置・尺度・符号の
affine不定性を除くが，未知の測定核を許す場合の$F\mapsto h(F)$という
非線形再パラメータ化までは除かない。その場合は測定族，calibration，
または作用素の追加制約が必要である。

以上より，有限潜在クラスでは，supported-pair lawsの回復，$W$を第三mode
とするanchor-pair tensorの一意分解，extension pair tensorsの線形反転により，
観測データ法則から未同時観測の項目集合を含む完全データ分布まで到達する。
これに対し，連続
因子に対する仮定3.2は位置・尺度・符号を正規化するだけであり，未知の測定核
を伴う連続潜在構造の識別定理ではない。連続因子を扱うには，非線形
再パラメータ化を除く測定族または作用素レベルの単射条件を別途与える必要が
ある。

\section{推定：bridge-weighted composite criterion}
識別結果を推定へ接続する。必要なsupported pairsの集合を
\[
\mathcal S_*=\{\{a,b\}\}\cup
\{\{j,a\}:j\notin\{a,b\}\}
\]
とする。第一段階では，各$S\in\mathcal S_*$について命題2.1の条件付き
moment equationを標本版へ置き換え，選択確率
$\pi_S(y,u)$を推定する。まず有限次元benchmarkとして
$\pi_S(y,u;\eta_S)$を仮定し，真値を$\eta_{S0}$と書く。柔軟な推定では，
$q_S^{-1}$をsieveで近似し，正値制約と正則化を課して同じmoment equationを
解く。いずれの場合も，推定したbridgeを別標本へ評価するcross-fittingを
用いることができる。

有限潜在クラスのパラメータを
\[
\theta=\{p,G,M_1,\ldots,M_m\}
\]
とし，確率単体制約とanchor orderingを課したパラメータ空間を$\Theta$と
する。$S$に対応する潜在測定モデルのpair densityを
\[
p_\theta(y_S\mid w,x)
=\sum_{f=1}^r\lambda_f(w,x;\theta)
 \prod_{j\in S}M_j(y_j,f;x;\theta)
\]
と書く。提案推定量は
\begin{equation}
\begin{aligned}
\widehat\theta=\arg\max_{\theta\in\Theta}\;&\frac1n\sum_{i=1}^n
 \sum_{S\in\mathcal S_*}c_S\\[-1mm]
&\frac{R_{iS}}{\widehat\pi_S(Y_{iS},U_i)}
 \log p_\theta(Y_{iS}\mid W_i,X_i).
\end{aligned}
\label{eq:composite-criterion}
\end{equation}
で定義する。$c_S>0$はblock間の尺度を調整する固定weightである。
これはfull likelihoodではなく，bridgeで回復したoverlapping pair lawsを
同時に当てはめるcomposite M-estimatorである。従って推論にはFisher
informationではなく，first-stageのbridge推定も含むsandwich分散を用いる。

\noindent\textbf{命題4.1（母集団criterionの一意性）.}
仮定1--7と3.1が成立し，$\theta_0\in\Theta$が正しくモデルを生成するとする。
さらに全ての$S\in\mathcal S_*$で$p_\theta(Y_S\mid W,X)$が共通support上で
正とする。このとき，\eqref{eq:composite-criterion}の母集団版は
$\theta_0$で一意に最大化される。

\noindent\textbf{証明.}
命題2.1のinverse-selection identityより，各weighted summandの期待値は
full pair lawの下での期待対数密度に等しい。従って真値との差は，各
$S\in\mathcal S_*$の条件付きKullback--Leibler divergenceの正の加重和で
ある。最大値が等しければ全てのanchor/extension pair lawsが一致し，
定理3.1の単射性より$\theta=\theta_0$である。\hfill$\square$

Zhao and Shao (2015)も識別可能なcomplete-case identityを疑似尤度へ
接続するが，本稿ではbridgeで回復した複数のpair lawsを一つの潜在測定
モデルへ制約付きで当てはめる点が異なる。この差が，単一回帰パラメータの
推定から，潜在クラス比率と全測定核の同時推定への拡張を担う。

\section{漸近理論}
まず有限次元bridgeを用いるregular benchmarkを完成させる。以下では
$(O_i)_{i=1}^n$は独立同分布であり，$\beta=(\theta,\eta)$，
$\eta=(\eta_S:S\in\mathcal S_*)$とする。

\noindent\textbf{仮定5.1（regularity and separation）.}
$\Theta$と各bridge parameter spaceはcompactで真値は内点にある。
criterionとfirst-stage momentsは真値近傍で二回連続微分可能で，必要な
envelopeは二乗可積分である。bridgeのJacobian，composite scoreのJacobian，
および対応するGodambe行列は非特異である。さらにclass probability，
supported-pair propensity，anchor tensorの最小特異値，Kruskal rankを
支えるminor，およびanchor orderingの隣接gapは，全て0から一様に離れる。

\noindent\textbf{定理5.1（有限次元bridgeの一致性と漸近正規性）.}
仮定5.1，命題4.1，およびfirst-stage moment estimatorの局所一意性の下で，
\[
\widehat\theta\xrightarrow{p}\theta_0,
\qquad
\sqrt n(\widehat\theta-\theta_0)
\rightsquigarrow N(0,A^{-1}BA^{-\top}).
\]
ここで$A$はprofiled composite scoreの平均Jacobian，$B$はbridge推定の
influence contributionを含む同scoreの分散である。したがって標準誤差は
全段階のestimating equationsをstackしたsandwich estimatorで求める。
個体単位bootstrapを用いる場合も，各反復でbridge推定，latent model fitting，
anchor orderingによるlabel alignmentまでを全て再実行する。

定理5.1の一致性は，標本criterionの一様収束と命題4.1の一意最大化から，
漸近正規性はstacked estimating equationsの線形展開から従う。ここで
B-completenessはbridgeの識別を与えるが，無限次元bridgeの推定速度までは
与えない。sieve bridgeは一般にill-posed inverse problemとなるため，
正則化，source condition，近似誤差と確率誤差のrate条件が必要である。
従って本稿の現段階では，sieve版には一致性を主結果とし，通常の
$\sqrt n$正規性はregular functionalまたはorthogonal scoreを別途構成した
場合の拡張として扱う。

\section{計算アルゴリズム}
計算は識別証明と同じ順序で進める。
\begin{enumerate}[label=\textbf{Step \arabic*.},leftmargin=8mm,itemsep=0.25em]
\item 各$S\in\mathcal S_*$で有限次元または正則化sieve bridgeを推定し，
  propensityの下限，moment residual，effective sample sizeを確認する。
\item bridge weightからanchor tensor $\widehat{\mathcal T}_{abW}$と
  extension tensors $\widehat{\mathcal T}_{jaW}$を構成する。
\item anchor tensorのnonnegative CP分解をspectral initializationと複数の
  初期値から計算し，各列を確率ベクトルへ正規化する。
\item anchor scoreの順序でclass labelを固定し，$\widehat p,\widehat G,
  \widehat M_a,\widehat M_b$を得る。
\item $\widehat G\odot\widehat M_a$を用いて各extension tensorを反転し，
  $\widehat M_j$を初期化する。小さい特異値にはtruncated inverseまたは
  ridge regularizationを用いる。
\item 上記を初期値として\eqref{eq:composite-criterion}を確率単体制約の下で
  block-coordinate updateし，criterionとparameter changeの両方で収束を
  判定する。
\item 全候補解からcriterion最大の解を採用し，rank gap，anchor gap，
  propensity overlap，label stabilityを報告する。
\end{enumerate}
Zhao and Shao (2015)の二段階更新は，識別に用いた条件付き法則と推定対象を
分ける点で本アルゴリズムの先例となる。ただし本稿のcriterionはlatent
mixtureを含み一般に非凸なので，difference-of-concave更新をそのまま移植
せず，tensor initialization，複数初期値，制約付き更新を組み合わせる。

\section{Simulation studies}
simulationは識別結果の数値的なsanity checkに限定し，定理3.1と同じ有限
カテゴリlatent-class modelだけを扱う。Allman, Matias and Rhodes
(2009, Sections 3--5)の$r$-class, $p$-feature model
$M(r;\kappa_1,\ldots,\kappa_p)$に従い，
\[
P=\sum_{f=1}^r p_f\bigotimes_{j=1}^p p_{fj}
\]
とする。ここでは$n=500$，$r=2$，$p=4$とし，常時観測される$W$と
$Y_1,Y_2,Y_3$を二値featureとする。class比率は
$(p_1,p_2)=(0.55,0.45)$，$W$のclass-specific Bernoulli確率は
$(0.20,0.80)$，測定核は
\[
M=\begin{pmatrix}
0.15&0.80\\
0.25&0.75\\
0.20&0.85
\end{pmatrix}
\]
とした。Allman et al.はmodel classと識別条件を与えるが，Monte Carloの
数値parameter，推定量，biasまたはcoverage表は提示していない。従って上記は
同論文から転記した数値ではなく，単体内部でclass separationを持つ本稿固有の
calibrationである。

$Y_1$は常時観測とし，$Y_2,Y_3$の観測確率をそれぞれ
\[
P(D_j=1\mid Y_1,Y_j)
=\operatorname{expit}\{\alpha_j-(Y_1+Y_j)\},\quad j=2,3,
\]
とする。$\alpha_j$は平均item観測率が80\%となるよう調整する。このとき
$S_0=\{1,2\}$と$S_3=\{1,3\}$の平均観測率はそれぞれ約70\%，complete-case
率は50.9\%，最小pair propensityは0.475であった。各blockのshadow
$Z_S$は4カテゴリとし，class 1と2における確率をそれぞれ
$(0.55,0.25,0.15,0.05)$と$(0.05,0.15,0.25,0.55)$とした。欠測確率には
$Z_S$を直接入れないため，$R_S\indep Z_S\mid(Y_S,W)$が成立する。

比較するのは三手法だけである。complete-data oracleは欠測前の
$(W,Y_1,Y_2,Y_3)$へ同じlatent-class modelを当てはめる。MARは観測された
supported pairsへ欠測補正なしのcomposite likelihoodを当てはめる。提案法は
各blockで
\[
\E\{R_S/\pi_S(Y_S)\mid Z_S,W\}=1
\]
の標本momentから二次元logit bridgeを推定し，得られたinverse-selection
weightでpairwise latent-class modelを推定する。全手法で6初期値から最大の
criterionを選び，$M_1$の大小でlabelを整合させた。

\begin{center}
\begin{minipage}{\linewidth}
\centering
\includegraphics[width=0.96\linewidth]{simulation/figures/allman_bias_rmse_comparison.pdf}
\captionof{figure}{Allman型有限latent-class modelにおける100 Monte Carlo
replicationsの比較（$n=500$）。左は測定核$M_j$，右はlatent class比率
$p_f$の平均biasとroot mean squared error (RMSE)を示す。}
\label{fig:allman-simulation}
\end{minipage}
\end{center}

\begin{center}
\begin{minipage}{\linewidth}
\centering
\resizebox{\linewidth}{!}{%
\begin{tabular}{lrrrr}
\hline
&\multicolumn{2}{c}{$M_j$}&\multicolumn{2}{c}{$p_f$}\\
Method&Bias&RMSE&Bias&RMSE\\
\hline
Complete-data oracle&0.0036&0.0373&0.0029&0.0351\\
MAR&$-0.0474$&0.0724&$-0.0792$&0.0929\\
Proposed bridge&$-0.0027$&0.0466&$-0.0028$&0.0452\\
\hline
\end{tabular}}
\captionof{table}{Allman型simulationの主要結果。$M_j$は全item・class成分を
集約した平均biasとRMSE，$p_f$は第2 class比率のbiasとRMSEである。}
\label{tab:allman-simulation}
\end{minipage}
\end{center}

100回のpilotでは，complete-data oracleの$M_j$ RMSEは0.0373，$p_f$ RMSEは
0.0351であった。MNARをMARとして扱うと，それぞれ0.0724と0.0929へ悪化し，
平均biasも$-0.0474$と$-0.0792$になった。提案法ではRMSEが0.0466と0.0452，
平均biasが$-0.0027$と$-0.0028$となり，両識別対象についてoracleに近い。
これは提案法が常に高効率であることの証明ではないが，MNAR selectionを
補正しながら測定核と潜在class比率を同時に回復するという識別論の含意と
整合する。最終稿では1,000 replicationsへ拡張し，Monte Carlo errorを縮小
した上で報告値を確定する。

\section*{参考文献}
\begin{enumerate}[label={[\arabic*]},leftmargin=7mm,itemsep=0.35em,topsep=0.2em,
  parsep=0pt,partopsep=0pt]
\item Little, R. J. and Rubin, D. B. (2002). \textit{Statistical Analysis with
Missing Data}, 2nd ed. Hoboken, NJ: Wiley.
\item Robins, J. M. and Ritov, Y. (1997). Toward a curse of dimensionality
appropriate (CODA) asymptotic theory for semi-parametric models.
\textit{Statistics in Medicine}, 16, 285--319.
\item Greenlees, J. S., Reece, W. S., and Zieschang, K. D. (1982). Imputation
of missing values when the probability of response depends on the variable
being imputed. \textit{Journal of the American Statistical Association}, 77,
251--261.
\item Ibrahim, J. G., Chen, M.-H., and Lipsitz, S. R. (2001). Missing responses
in generalised linear mixed models when the missing data mechanism is
nonignorable. \textit{Biometrika}, 88, 551--564.
\item Qin, J., Leung, D., and Shao, J. (2002). Estimation with survey data under
nonignorable nonresponse or informative sampling. \textit{Journal of the
American Statistical Association}, 97, 193--200.
\item Kott, P. S. and Chang, T. (2010). Using calibration weighting to adjust
for nonignorable unit nonresponse. \textit{Journal of the American Statistical
Association}, 105, 1265--1275.
\item Kim, J. K. and Yu, C. L. (2011). A semiparametric estimation of mean
functionals with nonignorable missing data. \textit{Journal of the American
Statistical Association}, 106, 157--165.
\item Tang, G., Little, R. J., and Raghunathan, T. E. (2003). Analysis of
multivariate missing data with nonignorable nonresponse. \textit{Biometrika},
90, 747--764.
\item Zhao, J. and Shao, J. (2015). Semiparametric pseudo-likelihoods in
generalized linear models with nonignorable missing data. \textit{Journal of
the American Statistical Association}, 110(512), 1577--1590.
\href{https://doi.org/10.1080/01621459.2014.983234}{doi:10.1080/01621459.2014.983234}.
\item d'Haultfoeuille, X. (2010). A new instrumental method for dealing with
endogenous selection. \textit{Journal of Econometrics}, 154(1), 1--15.
\href{https://doi.org/10.1016/j.jeconom.2009.06.005}{doi:10.1016/j.jeconom.2009.06.005}.
\item Muth\'en, B. O., Kaplan, D., and Hollis, M. (1987). On structural
equation modeling with data that are not missing completely at random.
\textit{Psychometrika}, 52(3), 431--462.
\href{https://doi.org/10.1007/BF02294365}{doi:10.1007/BF02294365}.
\item Holman, R. and Glas, C. A. W. (2005). Modelling non-ignorable
missing-data mechanisms with item response theory models. \textit{British
Journal of Mathematical and Statistical Psychology}, 58(1), 1--17.
\href{https://doi.org/10.1111/j.2044-8317.2005.tb00312.x}{doi:10.1111/j.2044-8317.2005.tb00312.x}.
\item Lee, S.-Y. and Tang, N.-S. (2006). Bayesian analysis of nonlinear
structural equation models with nonignorable missing data.
\textit{Psychometrika}, 71(3), 541--564.
\href{https://doi.org/10.1007/s11336-006-1177-1}{doi:10.1007/s11336-006-1177-1}.
\item Kuha, J., Katsikatsou, M., and Moustaki, I. (2018). Latent variable
modelling with non-ignorable item non-response: Multigroup response propensity
models for cross-national analysis. \textit{Journal of the Royal Statistical
Society: Series A (Statistics in Society)}, 181(4), 1169--1192.
\href{https://doi.org/10.1111/rssa.12350}{doi:10.1111/rssa.12350}.
\item Allman, E. S., Matias, C., and Rhodes, J. A. (2009). Identifiability of
parameters in latent structure models with many observed variables.
\textit{The Annals of Statistics}, 37(6A), 3099--3132.
\href{https://doi.org/10.1214/09-AOS689}{doi:10.1214/09-AOS689}.
\end{enumerate}

\end{multicols}

\end{document}
